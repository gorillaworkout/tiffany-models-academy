export const metadata = {
  title: 'Login Member',
  description: 'Login ke portal member Tiffanny Models Academy.',
  robots: { index: false, follow: false },
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
